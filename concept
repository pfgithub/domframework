let $a = [5];
$a.push(6);

$a === $a // should return true
// do this by separating watchable from value


function $Component () {

return $.update(() => { // $.update will be handled by the compiler
    if(a === 5) return <a></a>
    return <div></div>
});
}

// don't actually do this, it's just a concept
static_if(a === 5); {

}; static_else(); {

}